trigger:
  branches:
    include:
      - main

pool:
  vmImage: ubuntu-latest

steps:
  - checkout: self

  - script: |
      echo "GitHub is the source of truth for this repository."
      echo "Azure Repos is maintained as a secondary mirror via local push automation."
      echo "No server-side mirror action is required."
    displayName: Mirror status
